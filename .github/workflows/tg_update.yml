name: CI
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository to the runner
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - shell: bash
        env:
          tg_bot_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          tg_channel_id: ${{ secrets.TELEGRAM_CHANNEL_ID }}
          #tg_chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          #!/bin/bash
          commit=$(git log -1 --pretty=format:"%H")
          echo $commit
          echo $(git diff-tree --no-commit-id --name-only -r $commit )
          echo $(git diff-tree -h)
          #Get the last file changed
          #last_file=$(echo $files_changed | tail -n 1)
          #echo $last_file
          #TG update
          #if [  $last_file ]; then
          #curl -s -X POST https://api.telegram.org/bot$tg_bot_token/sendMessage -d chat_id=$tg_channel_id -d parse_mode=Markdown --data-urlencode text@$last_file
          #fi
          #curl -s -X POST https://api.telegram.org/bot$tg_bot_token/sendMessage -d chat_id=$tg_chat_id -d text="New commit pushed to the repository"
